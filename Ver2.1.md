# BriefyPet Ver2.1

## 1. 历史 pending 文章缺少补打分机制

### 现象

- 部分文章已入库，由于应用退出、电脑关机等原因中断打分，但长期停留在 `score_status = 'pending'`
- 这些文章没有进入排序池，也不会继续参与后续推送

### 已验证原因

- 新文章入库后先标记为 `pending`
- 当前流程只处理“本轮刚抓到并放在内存里的文章”
- 缺少“从数据库重新捞出历史 pending 继续打分”的补偿逻辑

### 修改思路

- 增加历史 `pending` 文章的补打分机制
- 每轮抓取后补打分遗留 `pending`并切换状态

## 2. 首次配置后的首轮扫描未与日常增量扫描区分处理

### 现象

- 首次配置应用后，会先收到一小波个位数推送，质量偏低
- 过一会儿又会收到一大波 30-60 条推送，质量明显更高
- 而日常正常启动应用，推送内容和数量正常

### 已验证原因

- 首次配置完成后会立即触发扫描，而不是延后到首轮数据稳定后再推送
- 首轮扫描时会进入大量首次抓取文章，这些文章会先入库为 pending，再分批完成 LLM 打分
- 当前流程没有把“首次配置后的首轮全量扫描”与“日常启动后的增量扫描”区分处理
- 推送生成与展示没有等待首轮主批次基本完成，先完成的小批次结果会先进入推送
- 日常正常启动时，大多数源已存在抓取时间戳，通常只处理增量数据，因此推送结果更稳定、质量更高

### 修改思路

- 为首次配置设置单独逻辑，区分日常应用启动

## 3. 推送缺少最低质量门槛

### 现象

- 当前会出现明显低质量文章被推送
- 已发现存在 `fit_score = 5 / 10 / 20 / 40` 的内容进入推送

### 已验证原因

- 当前推送按 bucket 取 Top N
- 只校验发布时间窗口，没有最低分阈值
- 因此样本少时，低分内容也会被推出来

### 修改思路

- 增加推送最低分阈值 60，仅允许满足最低 `fit_score` 的内容进入推送

## 4. Windows 桌宠顶部提示框文字裁切

### 现象

- 部分内测机器上，桌宠顶部提示框第一行文字顶部被裁切
- 同样分辨率和缩放下，开发机正常，内测机异常

### 已验证原因

- 提示框字号、行高、留白较紧，布局对字体实际字形高度过于敏感
- 父容器使用了 `overflow: hidden`

### 修改思路

- 放宽提示框的 line-height 和 padding
- 评估增加顶部留白或放宽裁切边界

## 5. 边栏信息层级不清晰

- 主界面阅读界面左边栏Feed中Unread是单独的，Today/Favorites/History 是一个信息层级， 科技/社科/医学等module是一个信息层级，每个module下研究/官方/博客等bucket是下一个信息层级
- module层级与Today/Favorites/History这个层级没有做缩进区分

## 6. 首次打开体验断裂 & 缺乏首次配置引导

- 用户安装软件后双击不会打开桌宠出现在桌面上，需要在托盘中右键显示
- 没有单独的新手引导，建议做一个主页面之外的小窗提示，可以按步骤点下去或直接跳过，主界面上也保留“帮助”按钮，后续用户也还可以再点开看
- 引导页说明：
  - 欢迎与项目定位简介
  - 功能说明
  - APIKEY配置说明
  - 个人兴趣与源配置说明
  - 源码地址和开发者联系邮箱

## 7. 阅读界面体验

- bubble提示新内容时，点击“立即查看”会导致unread减少，每点一下就会少一条；“立即查看”应该只负责打开阅读页面
- bubble提示新内容时，点击“稍后30分钟”会导致unread立刻清零，要想中途打开看就不知道哪些是新的；建议依旧保留在unread里面，bubble数量提示仅统计unread里面数量，中途已读的后面就不提示
- 对于单条信息设置为未读不会出现在unread里面，只在前面有一个点进行提示；建议需要进入unread
- 从一篇文章切换到另一篇文章时，详情页视图会保留之前的滚轮位置，即前一篇阅读到末尾，切换到下一篇也还是末尾，我希望是从头读起；建议切换后详情页视图滚回顶部默认值

## 8. 轮询动画

- 现在只有轮询、搜索、提醒状态下桌宠出现在桌面上，轮询仍采用scanning和idle动画，用户可能不理解明明一段时间出现一次的意义是什么
- 有无可能轮询流程中的轮询露出状态设置为单独的状态，动画更换为现有素材中的clawd-idle-reading.gif，并小字提醒“轮询中”；判定有due，切换为扫描状态；没有due就检查有无waiting，有就切换提醒状态；都没有就结束轮询状态（轮询动画保持2秒，检查waiting优先级低于检查due）

## 9. 用户反复双击会导致运行多个进程，建议有一个运行再双击就不会开新进程

## 10. 桌宠默认出现位置建议设置为屏幕右下角