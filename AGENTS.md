# Briefy-pet - AGENTS.md

## 项目目标
Briefy-pet 是一个面向普通用户的信息雷达桌宠，核心目标是以桌宠形式展示高价值信息提醒，支持本地存储、低打扰提醒、个性化配置、RSS 源抓取等功能。

产品定位不是做“纯阅读器”，而是做“桌面上的信息提醒伙伴”：
- 平时以桌宠形态常驻桌面
- 有高契合度内容时，用气泡做低打扰提醒
- 用户需要进一步查看时，再进入主窗口统一管理内容

### 交付形态
- 第一版交付形态为 Windows 平台可安装的 `exe` 安装包，这个部分已经完成
- 目前在该分支，我们想最大化合作效率。保证前台中台后台逻辑不变，只改变交付形态为mac平台可安装的安装包。
- 用户最终拿到的应是可以直接安装和运行的桌面应用，而不是源码工程或命令行工具

---

## 参考项目
- **reference/WindowPet**：参考桌宠窗口实现、透明、无边框、置顶功能；参考标准 Tauri + Rust 项目架构
- **reference/Clawd on Desk**：参考桌宠状态机和外部事件驱动能力
- **reference/AionUi**：参考气泡提示交互（可拖动、可点击）
- **reference/BriefFeed**：参考多源 RSS 抓取、去重、过滤、摘要生成能力

---

## 第一版产品范围

### 明确纳入第一版的能力
- 桌宠常驻桌面，支持拖拽、置顶、托盘管理
- 气泡汇总提醒
- 主窗口阅读与收藏
- 内置 RSS 源抓取与开关管理
- 本地 SQLite 存储
- 用户填写自己的 LLM API Key
- 用户填写“兴趣偏好”文本
- LLM 负责摘要、契合度判断、推荐理由生成

### 第一版暂不实现
- 无 Key 模式
- 单独的新手引导页
- 用户自己新增 RSS 源
- 已读状态
- 复杂的多档提醒策略
- 稍后时长自定义

---

## 模块划分

### 1. 桌宠层（pet-window）
- **功能**：桌面上常驻的透明宠物窗口
- **行为**：支持拖拽、置顶、开机自启、任务栏隐藏
- **交互重点**：
  - 双击桌宠，默认打开主窗口的阅读界面
  - 如果当前处于“待配置”状态，双击桌宠直接打开设置页
- **视觉与形态要求**：
  - 桌宠整体形态优先参考 **reference/WindowPet**
  - 重点参考其“桌面常驻小体量角色”的呈现方式，而不是普通应用悬浮窗
  - 在透明、无边框、置顶、桌面陪伴感这几个方向上尽量贴近 WindowPet 的体验
- **实现**：使用 Tauri 实现透明窗口和窗口行为，React 渲染桌宠 UI
- **参考项目**：WindowPet

### 2. 气泡层（bubble-window）
- **功能**：展示汇总提醒气泡
- **行为**：
  - 气泡显示“你有 X 条新内容”
  - 提供三个按钮：`立即查看`、`稍后 30 分钟提醒`、`忽略本次`
  - 气泡不会自动消失，会一直存在，直到用户处理
  - 新的高契合度内容到达后，气泡中的数量直接更新
- **实现**：React + Tauri，监听 RSS 更新和提醒状态变化
- **参考项目**：AionUi

### 3. 主窗口层（main-window）
- **功能**：统一管理内容与设置
- **结构**：
  - 左侧为内容列表
  - 右侧为当前文章详情
- **左侧列表分组**：
  - `新内容`
  - `全部`
  - `收藏`
- **实现**：React 渲染界面，Tauri + Rust 处理本地存储与窗口协作
- **参考项目**：WindowPet

### 4. RSS 抓取后端（rss-fetcher）
- **功能**：定时抓取内置 RSS 源并进行内容处理
- **行为**：
  - 抓取 RSS 数据
  - 去重
  - 调用 LLM 生成摘要
  - 根据用户兴趣偏好生成契合度与推荐理由
- **提醒规则**：
  - 契合度分为：`高 / 中 / 低`
  - 只有契合度为 `高` 的内容进入气泡提醒
  - `中 / 低` 契合度内容进入列表，但不弹气泡
- **实现**：Rust 实现抓取与处理，SQLite 保存结果
- **参考项目**：BriefFeed

### 5. 数据库（db）
- **功能**：本地存储文章、提醒状态、用户设置
- **至少需要覆盖的数据类型**：
  - RSS 文章基础信息
  - LLM 摘要
  - 契合度结果
  - 推荐理由
  - 收藏状态
  - 新内容分组状态
  - 当前提醒批次状态
  - 用户设置（API Key、兴趣偏好、RSS 开关、开机启动）
- **实现**：SQLite
- **参考项目**：BriefFeed

---

## 开发顺序

### 第一阶段：桌宠、托盘、主窗口骨架
1. 实现桌宠常驻窗口，支持透明、拖拽、置顶、托盘显示
2. 实现主窗口基础结构：左侧列表、右侧详情、设置面板
3. 实现“待配置”状态与设置页跳转
4. 验证桌宠双击行为和主窗口切换逻辑

### 第二阶段：气泡提醒链路
1. 实现汇总气泡窗口
2. 实现 `立即查看 / 稍后 30 分钟 / 忽略本次` 三个操作
3. 实现新内容数量动态更新
4. 验证气泡与桌宠状态联动

### 第三阶段：RSS 抓取与 LLM 处理
1. 实现内置 RSS 源抓取
2. 实现去重与本地存储
3. 接入 LLM，生成摘要、契合度、推荐理由
4. 根据契合度高低决定是否进入气泡提醒

### 第四阶段：内容管理体验补全
1. 完善新内容、全部、收藏三类列表
2. 完成收藏与取消收藏逻辑
3. 打通“打开详情后移出新内容”的规则
4. 验证完整提醒链路和状态流转

---

## 交互流程

### 1. 启动流程
- 用户启动应用后，桌宠常驻桌面并置顶，仅在托盘中显示，不在任务栏显示
- 应用启动时先进入 **loading**
- 系统检查配置是否完整，尤其是 API Key 是否存在且可用
- 如果没有有效 API Key，则不进入后续抓取流程，桌宠进入 **needs-config（待配置）**
- 在 **needs-config** 状态下，双击桌宠直接打开设置页
- 配置完成后，桌宠进入 **scanning**，开始拉取和处理内容

### 2. 内容处理流程
- 后端按设定频率抓取内置 RSS 源
- 新内容进入去重和基础清洗流程
- LLM 基于用户的“兴趣偏好”文本生成：
  - 摘要
  - 契合度（高 / 中 / 低）
  - 推荐理由
- 契合度为 `高` 的内容进入当前提醒批次
- 契合度为 `中 / 低` 的内容进入主窗口列表，但不触发气泡

### 3. 新内容到达
- 当存在新的高契合度内容时，显示汇总气泡
- 气泡文案为：`你有 X 条新内容`
- 气泡中的 X 表示当前需要提醒的高契合度内容数量
- 如果提醒气泡已存在，后续新增高契合度内容时，气泡数量直接更新
- 桌宠状态切换为 **new-info**

### 4. 气泡操作

#### 立即查看
- 点击 `立即查看` 后，打开主窗口阅读界面
- 主窗口默认定位到“本次新内容里契合度最高的一条”
- 用户点开某篇内容详情后，这篇内容不再属于 `新内容`
- 该内容仍保留在 `全部` 列表中

#### 稍后 30 分钟
- 点击 `稍后 30 分钟` 后，仅延后“点击当下这批已进入气泡汇总的内容”
- 这批内容在 30 分钟后重新进入提醒
- 后续新到达的高契合度内容，不受这次稍后操作影响

#### 忽略本次
- 点击 `忽略本次` 后，当前这批内容不再弹出气泡
- 这些内容仍保留在主窗口列表中
- `忽略本次` 只影响提醒，不改变文章内容状态

### 5. 主窗口行为

#### 页面结构
- 顶部区域显示应用标题、桌宠状态、阅读 / 设置切换入口
- 阅读区域为双栏布局：
  - 左侧：内容列表
  - 右侧：当前文章详情
- 设置区域在主窗口内切换展示，不单独打开新页面

#### 左侧内容列表
- 仅分为三组：
  - `新内容`
  - `全部`
  - `收藏`
- 用户点开某篇内容详情后，这篇内容从 `新内容` 分组移出
- `全部` 保留所有已抓取内容
- `收藏` 只展示当前已收藏内容

#### 右侧详情区域
- 展示以下信息：
  - **标题**
  - **摘要**
  - **链接**
  - **来源**
  - **LLM 打分 / 契合度**
  - **LLM 推荐理由**

#### 内容状态
- 第一版内容状态只有两类：
  - `普通`
  - `收藏`
- 普通内容可以标记为收藏
- 收藏内容可以取消收藏，取消后回到普通
- 第一版不做“已读”状态

### 6. 设置页内容
- 设置页放在主窗口内部，不额外做独立引导页
- 至少包括以下配置项：
  - **我的兴趣偏好**：文本输入框，用户以自然语言描述自己感兴趣的方向
  - **API Key 设置**：用户输入并保存自己的 API Key
  - **RSS 源管理**：只管理内置 RSS 源的启用或禁用
  - **开机启动**：控制是否开机自启

### 7. 托盘交互
- 用户右键托盘图标时，可选择：
  - `隐藏`
  - `显示`
  - `主界面`
  - `退出`

---

## 桌宠状态定义
- **loading**：应用启动、加载本地配置时显示
- **needs-config**：缺少有效 API Key，无法进入抓取和 LLM 处理流程
- **scanning**：正在抓取和分析内容
- **idle**：当前无待提醒内容，处于等待状态
- **new-info**：存在高契合度新内容，且伴随汇总气泡提醒

---

## 关键产品规则

### API Key 规则
- 不允许无 Key 模式
- 没有有效 API Key 时，不进行抓取、摘要、契合度判断、推荐理由生成
- 此时桌宠可以显示，但必须停留在 `needs-config`

### 提醒规则
- 只推送契合度为 `高` 的内容
- 气泡为汇总提醒，不做单条提醒
- 气泡不自动消失
- 气泡数量可随新内容实时更新

### 内容规则
- 第一版不做已读
- “新内容”不等于“未读”，而是“用户还没有点开过详情的内容”
- 用户点开详情后，该内容移出 `新内容`，但仍保留在 `全部`

### 收藏规则
- 内容支持收藏和取消收藏
- 收藏只是内容归类，不影响提醒逻辑

### 忽略规则
- “忽略本次”只影响当前这批提醒
- 被忽略的内容不会再次弹出当前气泡
- 被忽略的内容仍然保留在内容列表中

---

## 注意事项
- 必须依赖 LLM API，不实现无 Key 模式
- 保证桌宠状态变化和交互响应足够快
- 产品结构需保持良好的分层，方便后续扩展
- 整体架构优先参考 **reference/WindowPet**
- 桌宠外观与桌面存在感要着重参考 **reference/WindowPet**，优先保证“像桌宠”而不是“像悬浮工具窗”
- 交付目标应始终指向 Windows 可安装的 `exe` 安装包
- 前端视觉和桌宠素材可以优先参考 `reference` 中的开源项目，保持风格一致
- RSS 源设计和抓取处理可参考 **reference/BriefFeed**
