# Briefy-pet Ver1-1

## 文档定位

本文档是基于 [AGENTS.md](/Users/air/Documents/BriefyPet/AGENTS.md) 的交付形态增量说明。

- Version1 的产品目标、功能范围、交互规则、状态定义、模块边界保持不变
- Ver1-1 只调整交付目标与构建验收标准，不重写原有产品需求
- 如与 Version1 文档存在冲突，以“交付形态”和“打包验收”相关内容为准；其余仍以 [AGENTS.md](/Users/air/Documents/BriefyPet/AGENTS.md) 为准

---

## Ver1-1 目标

在不改变当前前端、中台、后端业务逻辑的前提下，将 Briefy-pet 从“Windows 可安装 `exe`”切换为“macOS 本地可安装桌面应用”交付。

用户最终拿到的应该是可以直接安装和运行的 mac 应用，而不是源码工程、命令行工具或只可开发运行的调试包。

---

## 保持不变的内容

以下内容继续沿用 Version1，不在 Ver1-1 中变更：

- 桌宠常驻桌面、拖拽、置顶、托盘管理
- 气泡汇总提醒与三种操作
- 主窗口阅读 / 收藏 / 设置结构
- RSS 抓取、去重、LLM 摘要、契合度判断、推荐理由
- 本地 SQLite 存储
- API Key 必填规则
- “新内容 / 全部 / 收藏”列表规则
- 提醒批次、忽略、稍后 30 分钟等状态流转规则

---

## Ver1-1 唯一核心变化

### 交付形态

- 从 Windows 安装包切换为 macOS 安装包
- 目标分发物为 `.dmg`
- 同时保留 `.app` 作为构建过程中的标准应用包产物

### 平台目标

- 当前分支以 macOS 为唯一正式交付目标
- 不要求在本分支继续产出 Windows `exe`
- 不为了兼容双平台而改动现有业务需求和交互设计

---

## 技术落点

### 打包方式

- 继续使用 Tauri 作为桌面应用容器
- Rust + React 架构不变
- bundler 目标切换为 macOS `app` 与 `dmg`

### 运行与分发原则

- 本地开发仍使用 `npm run tauri -- dev`
- 正式构建通过 Tauri `build` 生成 macOS 安装产物
- 分发优先使用 `.dmg`

### 兼容性约束

- 不引入新的在线服务依赖
- 不新增额外遥测、埋点或网络调用
- 不因切平台而改变数据库结构和核心业务流程

---

## 验收标准

Ver1-1 完成时，至少满足以下条件：

1. 应用可在 macOS 本机完成构建。
2. 构建结果包含可直接分发的 `.dmg` 安装包。
3. 应用安装后可正常启动，并保留 Version1 既有桌宠、气泡、主窗口、RSS、设置等能力。
4. 本地 SQLite、RSS 资源文件、图标等运行所需资源被正确打入包内。
5. 用户拿到的是可安装运行的桌面应用，而不是源码目录或裸二进制。

---

## 当前实施范围

本轮优先做以下事情：

- 补齐 Ver1-1 版本说明文档
- 将仓库内打包配置从 Windows 目标切到 macOS 目标
- 同步更新 README 中的环境要求、构建命令与产物说明
- 验证当前工程至少具备 macOS 打包所需的基础配置

本轮不做：

- 改写 Version1 功能需求
- 新增产品功能
- 为 App Store 上架补充签名、公证、沙盒等发行流程
- 因平台切换而重构现有前后端逻辑
