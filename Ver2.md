# BriefyPet Ver2

## 文档定位

本文件合并自原 `Ver1-1.md` 与 `Ver2-14.md`，用于统一记录当前 `mac` 分支的版本目标、结构调整与产品化要求。

- 产品基础目标、桌宠状态、提醒规则等总原则仍以 `AGENTS.md` 为准。
- 本文聚焦当前版本的交付形态、信息架构、信源结构、设置页、记忆机制与产品表达。
- 旧版拆分文档不再继续维护。

## Ver2 总目标

在不破坏 BriefyPet 核心信息雷达能力的前提下，完成以下升级：

1. 交付形态从 Windows 可安装包切换为 macOS 可安装包。
2. 信源配置与数据结构从旧的 `module / bucket` 双层语义过渡到 `一级学科 / 二级学科 / group` 三层语义。
3. 设置页从工程化表单升级为产品化配置体验。
4. 记忆机制从粗粒度日级总结升级为更有洞察力的周级兴趣提炼。
5. 整体话术、视觉与品牌细节去除 AI 痕迹，统一为正式产品表达。

## 一、交付形态

### 目标产物

- 正式交付目标为 macOS `.dmg`
- 构建过程中保留 `.app`
- 用户最终拿到的是可安装、可启动的桌面应用，而不是源码工程或命令行工具

### 技术约束

- 继续使用 Tauri 作为桌面容器
- React + Rust + SQLite 基础架构不变
- 不因切换平台而重构业务逻辑
- 不新增遥测、埋点或额外在线依赖

### 验收标准

1. 应用可在 macOS 本机完成构建。
2. 构建结果包含 `.app` 与可分发 `.dmg`。
3. RSS 目录、SQLite、图标等运行资源被正确打入包内。
4. 安装后可正常打开桌宠、提醒、主窗口、帮助窗口与记忆回顾窗口。

## 二、当前主窗口信息架构

当前主窗口左栏结构统一为：

1. `Unread`
2. `Today`
3. `Favorites`
4. `History`

### 语义说明

- `Unread`：当前推送批次中尚未完成归档的内容
- `Today`：已从 `Unread` 归档并属于当天的内容
- `Favorites`：用户收藏或附带笔记的内容
- `History`：历史推送沉淀内容

### 交互约束

- 在 `Unread` 中同一条内容首次点击仅选中，第二次点击才归档到 `Today`
- 各分组支持按 `Module / Bucket` 或当前源分类聚合展开
- 中栏时间线按时间倒序展示
- 右栏详情区展示标题、摘要、来源、契合度、推荐理由、收藏与笔记状态

## 三、信源结构与抓取链路升级

### 信源目录现状

当前 RSS 目录文件为：

`src-tauri/resources/rss_catalog_0425.opml`

其语义不再是旧的 `module / bucket` 双层，而是：

- 一级学科
- 二级学科
- group

### 设置页需要同步的结构变化

1. 首次配置时，用户先勾选一级学科。
2. 在一级学科下继续展开勾选具体二级学科。
3. 每个一级学科都允许用户填写一句话兴趣偏好。
4. group 继续作为更细粒度的内容组织语义，不要求直接作为首层配置入口暴露。

### 抓取与时间规则

- 抓取单位从旧的 module 迁移为一级学科。
- 每个一级学科维护自己的 `last_scrape_time`。
- 每个 source 继续维护独立的 `last_source_time`。
- 科技类一级学科保持 6 小时一抓，其他学科保持 12 小时一抓。
- 关机重启后，默认重新启动全量扫描流程。
- 初始化抓取时间窗为过去两天；后续增量抓取以各 source 的上次抓取时间为基准。

### 沉淀池与推送池

- 打分完成后，内容先进入沉淀池。
- 每轮推送按一级学科执行 `topN` 选择，选中内容进入推送池。
- 推送到前端的唯一来源是推送池。
- 失败重爬成功后可进入沉淀池，但不直接触发新一轮推送。
- 沉淀池与推送池中，每个二级学科最多保留 300 条，清理时优先移除低分内容。

## 四、设置页产品化升级

当前设置页不应只是简单表单，而应接近正式产品的配置体验。

### 主要配置分区

1. LLM 配置
2. 信息雷达偏好
3. 记忆配置
4. 信源管理
5. 高级设置
6. 数据重置

### 设计要求

- 参考 Apple、VS Code、Notion 这类产品的设置页组织逻辑
- 信息层级清晰，分区明确，术语稳定
- 话术去除 AI 式堆砌和机械感
- 强调可读性、反馈感与操作的确定性

### 高级设置范围

- 新增 RSS 源时的完整三级归类
- 每个一级学科的推送 `topN`
- 抓取时间与频率控制
- 可删除的源管理
- 其他较少使用的实验项与系统级配置

## 五、记忆机制升级

### 当前问题

1. 周五晚九点的记忆回顾弹窗存在截断问题。
2. 现有提示词对用户兴趣的提炼过于机械，缺乏抽象与细化。
3. 输入范围过窄，只用了当天的星标与笔记。

### Ver2 要求

- 记忆输入范围升级为按周汇总。
- 输入内容至少包括：星标标题、摘要、用户批注。
- LLM 需要在用户原始一句话兴趣基础上，结合本周行为轨迹挖掘更细化、更抽象的兴趣点。
- 目标不是简单复述用户看过什么，而是沉淀可驱动后续筛选与推荐的真实偏好。

## 六、品牌与产品表达

### 基本要求

- 整体文字去除 AI 痕迹，保留克制、具体、产品化的表达方式。
- 配色、图标、界面氛围与 BriefyPet 的 IP 形象保持一致。
- 目前版本先不重做桌宠主体素材，但要同步优化页面配色、说明文案和品牌标识的一致性。

### 当前素材方向

- 桌宠 IP 参考：`public/pets/briefy-ip/main2.png`
- 应用图标与落地页品牌标识已统一为新的 SVG 源资产

## 七、当前实现与文档维护约定

- 版本说明统一维护在 `Ver2.md`
- README 负责说明项目整体定位、环境、运行与打包方式
- `AGENTS.md` 继续作为产品规则主文档
- 重大产品或交付变更需同步更新代码、README 与 Ver2
